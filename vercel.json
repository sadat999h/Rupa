{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/rupa run build && pnpm --filter @workspace/api-server run build:serverless",
  "outputDirectory": "artifacts/rupa/dist/public",
  "functions": {
    "artifacts/api-server/dist/serverless.mjs": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/artifacts/api-server/dist/serverless.mjs"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
